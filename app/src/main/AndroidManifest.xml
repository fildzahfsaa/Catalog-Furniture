<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.fildzah.catalog_furniture">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Product8Activity"></activity>
        <activity android:name=".Product7Activity" />
        <activity android:name=".Product6Activity" />
        <activity android:name=".Product5Activity" />
        <activity android:name=".Product4Activity" />
        <activity android:name=".Product3Activity" />
        <activity android:name=".Product1Activity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".Product2Activity" />
        <activity android:name=".adapters.ProductAdapter" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>